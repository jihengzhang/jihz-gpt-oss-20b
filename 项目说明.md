# 项目说明

## 项目概述
gpt-oss 是 OpenAI 开发的开源 GPT 模型系列，专注于强大的推理、代理任务和开发者用例。该项目提供开放权重的模型（如 gpt-oss-120b 和 gpt-oss-20b），支持多种后端部署，并采用 Harmony Response Format 进行交互。项目采用混合语言结构（Python、C++ 等），便于在不同硬件平台上运行。

## 项目结构
项目采用模块化设计，主要目录和文件如下：

- **根目录**：
  - `README.md`：项目文档和使用指南。
  - `pyproject.toml`：Python 项目配置（依赖、构建）。
  - `CMakeLists.txt`：C++ 构建配置，用于编译 Metal 后端。
  - `LICENSE`、`MANIFEST.in`、`USAGE_POLICY`：许可证和分发相关。
  - `awesome-gpt-oss.md`：资源汇总列表。

- **核心代码目录**：
  - `gpt_oss/`：主要 Python 包。
    - `__init__.py`、`chat.py`、`generate.py`、`tokenizer.py`：核心模块，用于聊天、生成和分词。
    - `evals/`：评估套件，包括 AIME、GPQA、HealthBench 等评估脚本和报告生成。
    - `metal/`：Metal 后端实现（Apple 设备专用），包含 C++ 源代码、Metal 着色器和 Python 绑定。
    - `responses_api/`：API 服务器相关，包括事件处理、类型定义和推理逻辑。
    - `tools/`：工具集成，如补丁应用、Python Docker、简单浏览器。
    - `torch/`、`triton/`、`vllm/`：不同推理后端的实现（PyTorch、Triton、VLLM）。

- **其他目录**：
  - `examples/`：示例代码，包括 Jupyter Notebook（强化微调）、JavaScript/Python SDK、Gradio/Streamlit 聊天界面。
  - `gpt-oss-mcp-server/`：MCP（Model Context Protocol）服务器，用于模型上下文管理。
  - `compatibility-test/`：兼容性测试（TypeScript 项目）。
  - `tests/`：单元测试和集成测试。
  - `tests-data/`：测试数据。
  - `docs/`：文档。
  - `_build/`：构建后端（自定义构建系统）。

- ### Python env
  - use conda env list 管理python env

项目结构支持多后端扩展，便于在 GPU、Apple Silicon 等硬件上运行。

## 后端部署方式对比

以下表格对比不同后端部署方式的特性：

| 后端       | 描述                                                                 | 适用场景                          | 优势                          | 劣势                          | 硬件要求                  |
|------------|----------------------------------------------------------------------|-----------------------------------|-------------------------------|-------------------------------|---------------------------|
| **torch**  | 非优化 PyTorch 实现，使用基本 PyTorch 运算符展示模型架构，支持张量并行。 | 教育和研究，了解模型内部结构。   | 简单易懂，易于修改和学习。   | 效率低，内存占用大。         | 需要 4× H100 GPU（gpt-oss-120b）。 |
| **triton** | 优化实现，使用 Triton MoE 内核和 CUDA 图，支持 MXFP4 量化。         | 单 GPU 高性能推理。              | 优化内存，单 GPU 运行大模型。 | 需要安装 Triton 源码，复杂。 | 单 80GB GPU（如 H100）。  |
| **vllm**   | 基于 VLLM 的高效推理引擎，支持批处理和采样优化。                   | 生产环境推理，支持并发请求。     | 高吞吐量，易集成。           | 依赖 VLLM 库，可能有兼容性问题。 | GPU 支持，内存充足。     |
| **ollama** | 外部工具，用于在消费者硬件上运行模型。                             | 本地部署，低资源环境。           | 简单易用，无需复杂配置。     | 性能可能不如专用后端。       | 消费者硬件（如 CPU/GPU）。 |
| **Metal**  | Apple Metal 框架实现，专为 Apple Silicon 优化。                      | macOS/iOS 设备。                 | 硬件加速，节能。             | 仅限 Apple 设备。            | Apple Silicon。           |
| **transformers** | Hugging Face Transformers 库集成，支持标准模型加载和推理。         | 快速原型和兼容性测试。           | 生态丰富，易用。             | 可能不如专用后端优化。       | GPU/CPU，支持量化。       |

## 深度学习框架详细对比

为了帮助您更好地理解，本章节详细介绍并对比 torch (PyTorch)、TensorFlow、transformers 和 vLLM。这些是深度学习领域常用的框架/库，尤其在AI模型开发和推理中。

### 框架简介
- **torch (PyTorch)**: 开源深度学习框架，由Meta开发。核心是动态计算图，支持灵活的模型构建和调试。项目中的 `torch/` 目录提供非优化的PyTorch实现，用于教育目的。
- **TensorFlow**: Google开发的开源框架，采用静态图（Graph模式），擅长生产部署和大规模训练。支持Keras API简化使用。
- **transformers**: Hugging Face开发的库，专注于Transformer模型（如GPT、BERT）。提供预训练模型、数据集和工具，便于NLP任务。
- **vLLM**: 专为大语言模型（LLM）推理优化的库，支持高效批处理和内存管理。项目中的 `vllm/` 目录集成此库，用于高性能推理。



### 总结
- 如果您是初学者或需要灵活实验，推荐从 **torch (PyTorch)** 开始。
- 对于生产级部署，**TensorFlow** 或 **vLLM** 更合适。
- **transformers** 适合快速使用预训练模型，而 **vLLM** 专攻推理性能。
- 在gpt-oss项目中，torch用于学习架构，vLLM用于高效运行，其他可作为替代。

如果您有具体问题或想运行示例，请告诉我！

## Harmony Response Format 用户模式对比

Harmony Response Format 是 gpt-oss 专用的响应格式，用于结构化对话交互。它支持以下用户模式（角色）：

| 模式       | 描述                                                                 | 区别说明（重点：User vs Assistant） |
|------------|----------------------------------------------------------------------|-------------------------------------|
| **System** | 系统级消息，用于设置模型的全局行为和规则。                         | -                                   |
| **Developer** | 开发者指令，用于提供模型的开发指南或约束。                       | -                                   |
| **User**   | 用户输入，表示人类用户的查询或指令。                               | User 是外部输入，驱动对话；Assistant 是模型响应。User 消息是对话的起点，Assistant 根据 User 输入生成回复。 |
| **Assistant** | 模型生成的响应，包括文本回复、工具调用等。                       | Assistant 是模型的输出，基于 User 输入生成；它可以包含推理过程、工具调用或最终答案。User 和 Assistant 交替形成对话流。 |
| **Tool**   | 工具调用，表示模型调用外部工具（如浏览器、Python 执行）的结果。   | -                                   |

**User 和 Assistant 的区别**：
- **User**：代表人类用户的输入，是对话的触发点。模型不会生成 User 消息，它是外部提供的。
- **Assistant**：代表模型的输出，是对 User 输入的响应。Assistant 消息可以包含文本、推理链、工具调用等，并支持结构化解析（如通过 Harmony 编码解析为消息条目）。

## MXFP 量化介绍

MXFP（Microsoft Floating Point）是 Microsoft 开发的量化格式，用于减少模型权重和激活的精度，从而降低内存使用和计算成本，同时保持模型性能。gpt-oss 使用 MXFP4 量化 MoE 权重，使模型能在有限内存中运行。

- **MXFP4**：4 位量化，将浮点数压缩到 4 位表示。适用于内存受限场景，如单 GPU 运行大模型（gpt-oss-120b 在 80GB GPU 上）。优势：显著减少内存（约 1/8），但可能略微影响精度。
- **MXFP8**：8 位量化，平衡精度和效率。比 FP16 更节省空间，但精度高于 MXFP4。适用于中等资源环境。
- **MXFP16**：16 位量化，接近标准 FP16，但使用 MXFP 格式优化。提供更高精度，适用于需要高质量输出的场景，但内存占用高于 MXFP4/MXFP8。

这些量化格式通过后训练量化实现，帮助模型在不同硬件上高效部署。

## Conda 和 uv 的对比

Conda 和 uv 都是Python包管理工具，但用途和设计不同。Conda 是Anaconda生态的通用包管理器，而uv 是新兴的快速包安装器。以下是详细对比。

### 工具简介
- **Conda**: Anaconda 开发的包管理器，支持创建隔离环境、安装包和依赖管理。它不仅限于Python，还支持R、C++等语言。常用于数据科学和机器学习。
- **uv**: 由Astronomer开发的现代Python包管理器，专注于速度和兼容性。它使用Rust编写，比pip快得多，支持pyproject.toml和PEP 621标准，类似于poetry或pip-tools。

### 对比表格

| 工具   | 描述                                                                 | 优势                          | 劣势                          | 适用场景                      | Python版本管理 | 安装方式                  | 环境交互                      |
|--------|----------------------------------------------------------------------|-------------------------------|-------------------------------|-------------------------------|----------------|---------------------------|-------------------------------|
| **Conda** | 通用包和环境管理器，支持多语言，创建虚拟环境，解决复杂依赖。       | 环境隔离强，生态丰富，支持非Python包 | 安装慢，磁盘占用大           | 数据科学、科研、复杂项目     | 支持（安装/切换版本） | conda install / conda env | 原生支持多环境，隔离强       |
| **uv**  | 快速Python包安装器，兼容pip，支持pyproject.toml和锁定文件。         | 极快安装（Rust加速），轻量，兼容标准 | 仅限Python，不支持多语言环境 | 现代Python项目、快速原型     | 不支持（依赖外部工具如pyenv） | uv pip install / curl安装uv | 识别当前环境（如conda），安装到激活环境 |

### 总结
- 如果您需要多语言支持或复杂环境管理，选择 **Conda**。
- 如果项目是纯Python且注重速度，选择 **uv**（尤其在gpt-oss这样的项目中，pyproject.toml已存在）。
- 在gpt-oss项目中，推荐使用uv进行包安装，因为它与pyproject.toml兼容且更快。
- **uv 特性**：uv 是 pip 的加速替代品（快10-100倍），支持虚拟环境，但不管理Python版本。安装uv用 `curl -LsSf https://astral.sh/uv/install.sh | sh`。`uv pip install` 直接用uv安装包，而非系统pip。在激活conda环境时，uv会自动安装到该环境。`pip list` 可列出当前环境包。uv不支持安装/切换Python版本，需用pyenv等工具。

## Docker的作用和机制

Docker 是一种开源的容器化平台，用于将应用程序及其依赖打包成轻量、可移植的容器。容器可以在任何支持Docker的环境中运行，确保应用的一致性和可移植性。

### Docker的作用
- **环境隔离**：容器提供独立的运行环境，避免主机系统和应用间的冲突。
- **可移植性**：容器可以在不同操作系统和云平台上无缝运行。
- **版本控制**：镜像版本化，便于回滚和部署。
- **资源效率**：相比虚拟机，容器更轻量，启动更快。
- **简化部署**：打包应用和依赖，一键部署。

### Docker的机制
- **镜像（Image）**：只读模板，包含应用代码、运行时、库和依赖。基于分层构建，每层可复用。
- **容器（Container）**：镜像的运行实例，可读写。容器是隔离的进程。
- **Dockerfile**：文本文件，定义如何构建镜像的指令。
- **Docker Engine**：运行容器的核心组件，包括守护进程和CLI。
- **注册表（Registry）**：存储和分发镜像，如Docker Hub。

Docker 使用Linux内核的cgroups和namespaces实现隔离，通过UnionFS实现分层文件系统。

## Docker build image的详细步骤

构建Docker镜像需要编写Dockerfile，然后使用`docker build`命令。以下是详细步骤：

### 1. 准备工作
- 安装Docker：`sudo apt-get install docker.io`（Linux）或下载Docker Desktop。
- 创建项目目录，放入应用代码、依赖文件（如requirements.txt）。

### 2. 编写Dockerfile
Dockerfile是构建镜像的脚本。示例（基于gpt-oss项目）：

```dockerfile
# 使用Python 3.12基础镜像
FROM python:3.12-slim

# 设置工作目录
WORKDIR /app

# 复制依赖文件并安装
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目代码
COPY . .

# 暴露端口（可选）
EXPOSE 8000

# 设置启动命令
CMD ["python", "-m", "gpt_oss.responses_api.serve", "--inference-backend", "vllm"]
```

### 3. 构建镜像
在项目目录运行：
```
docker build -t gpt-oss-image .
```
- `-t`：指定镜像标签。
- `.`：当前目录的Dockerfile。

### 4. 验证镜像
- 列出镜像：`docker images`
- 运行容器：`docker run -p 8000:8000 gpt-oss-image`
- 检查日志：`docker logs <container_id>`

### 5. 优化和高级步骤
- **多阶段构建**：减少镜像大小，使用多个FROM语句。
- **.dockerignore**：排除不必要文件，如.git。
- **缓存层**：合理排序指令，利用缓存加速重建。
- **推送镜像**：`docker tag gpt-oss-image username/gpt-oss:v1`，然后`docker push username/gpt-oss:v1`到注册表。
- **运行选项**：`-v`挂载卷，`-e`设置环境变量，`--rm`运行后删除容器。

通过这些步骤，您可以将Python环境、已安装库和项目文件夹打包到镜像中，确保一致的部署。

---

## 相关文档指南

**vLLM 安装和故障排查**：见 [troubleshooting.md](troubleshooting.md)
- 包含完整的安装历史记录和依赖冲突解决方案
- 常见问题诊断和快速排查命令

**部署脚本优化总结**：见 [DEPLOYMENT_SUMMARY.md](DEPLOYMENT_SUMMARY.md)

---
## 部署脚本优化总结

### 一、历史改进记录

#### 问题 1: uv 包管理器不稳定导致网络超时
- **症状**: `uvloop==0.22.1` 下载失败，网络超时（30s限制）
- **原因**: uv的HTTP超时设置不够长，大型包（如torch、vllm）需要更多下载时间
- **解决**: 改用标准pip工具 + 增加超时时间到300秒 → 最终优化为180秒（3分钟）

#### 问题 2: 国外镜像下载速度慢
- **症状**: 从官方PyPI下载大型包速度极慢，易中断
- **原因**: 网络延迟和国际带宽限制
- **解决**: 配置清华大学镜像 + 提供多个国内镜像备选方案

#### 问题 3: 安装过程缺乏可视化反馈
- **症状**: 大型包下载时无法判断进度，不知道是否挂掉
- **原因**: pip默认不显示详细日志
- **解决**: 添加 `-vv` 参数启用超详细日志输出

#### 问题 4: 脚本硬编码配置不灵活
- **症状**: 难以更换镜像和调整超时参数
- **原因**: 镜像URL和超时时间分散在各个install命令中
- **解决**: 将配置集中到脚本顶部，便于修改和维护

### 二、脚本优化总结</content>
<parameter name="filePath">/home/tester/AI_Tool/gpt-oss/项目说明.md


# vLLM gpt-oss-20b 部署脚本优化总结

## 一、历史改进记录

### 问题 1: uv 包管理器不稳定导致网络超时
- **症状**: `uvloop==0.22.1` 下载失败，网络超时（30s限制）
- **原因**: uv的HTTP超时设置不够长，大型包（如torch、vllm）需要更多下载时间
- **解决**: 改用标准pip工具 + 增加超时时间到300秒 → 最终优化为180秒（3分钟）

### 问题 2: 国外镜像下载速度慢
- **症状**: 从官方PyPI下载大型包速度极慢，易中断
- **原因**: 网络延迟和国际带宽限制
- **解决**: 配置清华大学镜像 + 提供多个国内镜像备选方案

### 问题 3: 安装过程缺乏可视化反馈
- **症状**: 大型包下载时无法判断进度，不知道是否挂掉
- **原因**: pip默认不显示详细日志
- **解决**: 添加 `-vv` 参数启用超详细日志输出

### 问题 4: 脚本硬编码配置不灵活
- **症状**: 难以更换镜像和调整超时参数
- **原因**: 镜像URL和超时时间分散在各个install命令中
- **解决**: 将配置集中到脚本顶部，便于修改和维护

## 二、脚本优化总结

### 核心改动

| 项目 | 原方案 | 新方案 | 优势 |
|------|--------|--------|------|
| 包管理器 | uv pip | pip | 更稳定，标准工具，支持更好 |
| 超时时间 | 30秒 | 180秒（3分钟） | 大型包（torch:2.5GB）不会超时 |
| 镜像源 | 硬编码 | 变量配置 | 一处修改全局生效，易于切换 |
| 日志级别 | 默认 | -vv详细 | 实时看到下载进度，问题可追踪 |
| 错误处理 | 无 | 状态检查+提示 | 失败时显示备选镜像 |

### 配置变量（脚本顶部）

```bash
# 国内镜像（4个选项，默认清华）
PIP_MIRROR="https://pypi.tuna.tsinghua.edu.cn/simple"

# 网络参数
PIP_TIMEOUT=180      # 3分钟
PIP_RETRIES=5        # 失败重试5次
```

### 更换镜像方法

若清华镜像不可用，编辑脚本第7行，改用其他镜像：

```bash
# 中科大（USTC）
PIP_MIRROR="https://mirrors.ustc.edu.cn/pypi/web/simple"

# 阿里云
PIP_MIRROR="https://mirrors.aliyun.com/pypi/simple/"

# 腾讯云
PIP_MIRROR="https://mirrors.cloud.tencent.com/pypi/simple"
```

## 三、使用指南

### 快速安装（推荐）
```bash
# 一步到位：创建环境 + 安装vLLM + 启动服务器
. deploy_vllm_gpt_oss_20b.sh --all
```

### 分步安装
```bash
# 仅创建环境
. deploy_vllm_gpt_oss_20b.sh --create-env

# 环境创建后再安装vLLM（激活环境）
conda activate gpt-oss-20b
. deploy_vllm_gpt_oss_20b.sh --install-vllm

# 安装gpt-oss包
. deploy_vllm_gpt_oss_20b.sh --install-gpt-oss

# 启动服务器
. deploy_vllm_gpt_oss_20b.sh --start-server
```

### 故障排查

**问题：下载卡住**
- 脚本会显示 `-vv` 详细日志，可看到正在下载的文件
- 默认3分钟超时，若还是失败，检查网络或更换镜像

**问题：当前镜像不可用**
- 脚本失败时会提示4个镜像选项
- 编辑脚本改换 `PIP_MIRROR` 变量后重试

**问题：依赖冲突**
- 脚本使用 `pyproject.toml` 的版本指定，已预先解决冲突
- 若仍有问题，检查 `pyproject.toml` 中的 `[project.optional-dependencies]` 配置

## 四、脚本架构说明

### 条件判断逻辑
- 每个步骤都检查是否已安装（避免重复）
- 支持单独执行任何步骤
- 所有步骤可组合运行

### 错误处理
- 捕获安装命令的退出码（`$?`）
- 失败时显示友好提示和解决方案
- 不自动继续，防止级联故障

### 环境激活
- `--create-env` 创建环境后自动激活
- 后续步骤检查 `$CREATE_ENV` 决定是否需要激活
- 支持已有环境的场景（无需重新创建）

## 五、对应的pyproject.toml优化

`pyproject.toml` 中vllm依赖组定义：

```toml
vllm = [
  "vllm==0.13.0",
  "torch==2.9.0",
  "torchaudio==2.9.0",
  "torchvision==0.24.0",
  "transformers>=4.57.0,<5.0.0",
  "tokenizers>=0.22.0",
  "numpy>=1.24.0,<2.3",
  "outlines_core==0.2.11",
]
```

所有版本已精确指定，通过镜像下载时无版本冲突问题。

## 六、性能指标

- **镜像源速度**: 清华镜像约 12-20 MB/s（国内网络）
- **完整安装时间**: 10-15分钟（包括torch 2.5GB下载）
- **重试机制**: 最多5次，自动恢复中断下载

## 七、后续可优化方向

1. ✓ 支持离线安装（保存wheels到本地）
2. ✓ Docker容器化打包（避免环境差异）
3. ✓ 并行下载多个包（加快速度）
4. ✓ 自动镜像选择（检测最快的镜像）

---

## 附加内容：完整安装指南

### 快速开始（推荐）

#### 一键部署：5分钟完成全部配置

```bash
cd /home/tester/jihz-gpt-oss-20b
. deploy_vllm_gpt_oss_20b_optimized.sh --all
```

**这个命令会自动完成：**
1. ✓ 创建 conda 环境（Python 3.12）
2. ✓ 安装 vLLM 0.13.0 及所有依赖（使用国内镜像加速）
3. ✓ 安装 gpt-oss 包
4. ✓ 启动 vLLM 服务器（http://localhost:8010）

**完整耗时：** 10-15分钟（取决于网络速度）

---

### 配置管理

#### 镜像源配置

**默认**: 清华大学镜像（中国最快）

编辑脚本第 15 行更换镜像源：

```bash
# 打开脚本
nano deploy_vllm_gpt_oss_20b_optimized.sh

# 修改 PIP_MIRROR 变量为：
# 中科大
PIP_MIRROR="https://mirrors.ustc.edu.cn/pypi/web/simple"

# 或阿里云
PIP_MIRROR="https://mirrors.aliyun.com/pypi/simple/"

# 或腾讯云
PIP_MIRROR="https://mirrors.cloud.tencent.com/pypi/simple"
```

#### 网络参数微调

若网络不稳定，编辑脚本第 16-17 行：

```bash
PIP_TIMEOUT=300   # 改为5分钟
PIP_RETRIES=10    # 改为重试10次
```

---

### 验证安装

#### 1. 检查 vLLM

```bash
conda activate gpt-oss-20b
python3 -c "import vllm; print(f'✓ vLLM {vllm.__version__}')"
```

预期输出: `✓ vLLM 0.13.0`

#### 2. 检查关键依赖

```bash
python3 << 'EOF'
import torch
import transformers
import vllm
print(f'torch: {torch.__version__}')
print(f'transformers: {transformers.__version__}')
print(f'vllm: {vllm.__version__}')
print(f'CUDA available: {torch.cuda.is_available()}')
EOF
```

预期输出:
```
torch: 2.9.0
transformers: 4.57.3
vllm: 0.13.0
CUDA available: True
```

---

### 启动服务

#### 标准启动

```bash
conda activate gpt-oss-20b
vllm serve openai/gpt-oss-20b --port 8010
```

#### 后台运行

```bash
# 使用 nohup
nohup vllm serve openai/gpt-oss-20b --port 8010 > server.log 2>&1 &

# 使用 tmux
tmux new-session -d -s vllm-server "conda activate gpt-oss-20b && vllm serve openai/gpt-oss-20b --port 8010"

# 查看日志
tail -f server.log
```

---

## 常见问题

### Q: 安装需要多长时间?
A: 首次安装 10-15 分钟（取决于网络和硬件）。后续更新会更快。

### Q: 可以离线安装吗?
A: 可以，先在有网络的机器上下载 wheels，再传输到离线环境。

### Q: 支持哪些 GPU?
A: 支持 NVIDIA GPU（计算能力 ≥ 7.0），如 V100, A100, RTX 系列。

### Q: 如何使用自定义模型?
A: `vllm serve ./path/to/local/model --port 8010`

### Q: 如何启用调试日志?
A: `VLLM_LOG_LEVEL=DEBUG vllm serve openai/gpt-oss-20b --port 8010`

---

**最后更新**: 2026年1月13日  
**维护者**: gpt-oss 项目团队


# vLLM 安装记录

## 概述
本文档记录了在 gpt-oss 项目中成功安装 vLLM 0.13.0 的完整过程和解决的依赖冲突问题。

## 环境信息
- **操作系统**: Linux
- **Python 版本**: 3.13.11
- **环境类型**: Conda (gpt-oss-env)
- **vLLM 目标版本**: 0.13.0

## 安装过程

### 第一步：优化项目依赖结构

#### 问题分析
原始 `pyproject.toml` 将所有依赖（包括 FastAPI、Docker、Jupyter 等重量级包）都放在核心依赖中，导致：
- 基础安装过于臃肿
- 不同后端的依赖混在一起
- 缺少版本上限约束，容易出现破坏性更新

#### 解决方案
重构 `pyproject.toml`，采用模块化依赖管理：

**核心依赖（最小化）**:
```toml
dependencies = [
  "openai-harmony>=0.0.8",
  "tiktoken>=0.9.0",
  "aiohttp>=3.12.14",
  "chz>=0.3.0",
  "pydantic>=2.11.7,<3.0.0",
  "structlog>=25.4.0",
  "tenacity>=9.1.2",
  "requests>=2.31.0",
  "termcolor>=2.0.0",
]
```

**可选依赖组分类**:
- `server`: FastAPI + Uvicorn (API 服务)
- `web`: Docker + html2text + lxml (网页工具)
- `jupyter`: Jupyter 集成
- `torch`: PyTorch 推理后端
- `triton`: Triton 推理后端
- `vllm`: vLLM 高性能服务
- `metal`: Apple Silicon 支持
- `test`: 测试工具
- `eval`: 评估框架
- `mcp`: MCP 服务器支持
- `all`: 完整安装
- `dev`: 开发工具

### 第二步：初次安装 vLLM

执行命令：
```bash
pip install vllm==0.13.0
```

成功安装的关键包：
- vllm==0.13.0
- torch==2.9.0
- transformers==4.57.3
- triton==3.5.0
- 大量 NVIDIA CUDA 相关库（cublas, cudnn, cusparse 等）

### 第三步：解决依赖冲突

#### 遇到的冲突

**冲突 1: outlines_core 版本冲突**
```
outlines 0.1.11 requires outlines_core==0.1.26
vLLM 0.13.0 requires outlines_core==0.2.11
```

**冲突 2: xformers 与 torch 版本不匹配**
```
xformers 0.0.30 requires torch==2.7.0
但安装的是 torch==2.9.0 (vLLM 要求)
```

#### 解决步骤

1. **分析 vLLM 的精确依赖**:
   ```bash
   pip show vllm | grep Requires
   ```
   
   发现 vLLM 依赖 `outlines_core` 但不依赖 `outlines`

2. **移除冲突的包**:
   ```bash
   pip uninstall outlines xformers -y
   ```

3. **强制安装精确版本的 PyTorch 生态**:
   ```bash
   pip install "torch==2.9.0" "torchaudio==2.9.0" "torchvision==0.24.0" \
               "numpy<2.3,>=1.24" --force-reinstall --no-deps
   ```

4. **验证依赖完整性**:
   ```bash
   pip check
   ```
   输出: `No broken requirements found.`

### 第四步：更新依赖配置

在 `pyproject.toml` 中添加精确的 vLLM 依赖组：

```toml
[project.optional-dependencies]
# vLLM inference backend (high-performance serving)
# Note: vLLM has strict version requirements, use exact versions to avoid conflicts
vllm = [
  "vllm==0.13.0",
  "torch==2.9.0",
  "torchaudio==2.9.0",
  "torchvision==0.24.0",
  "transformers>=4.57.0,<5.0.0",
  "tokenizers>=0.22.0",
  "numpy>=1.24.0,<2.3",
  "outlines_core==0.2.11",
]
```

## 最终安装的关键版本

| 包名 | 版本 | 说明 |
|------|------|------|
| vllm | 0.13.0 | 核心推理引擎 |
| torch | 2.9.0 | PyTorch (vLLM 严格要求) |
| torchaudio | 2.9.0 | 音频处理库 |
| torchvision | 0.24.0 | 视觉处理库 |
| transformers | 4.57.3 | Hugging Face 模型库 |
| triton | 3.5.0 | GPU 内核优化 |
| numpy | 2.2.6 | 数值计算 (< 2.3) |
| outlines_core | 0.2.11 | 结构化生成 (vLLM 要求) |
| tokenizers | 0.22.2 | 快速分词器 |

## 关键经验总结

### 1. 依赖冲突的根本原因
- **outlines vs outlines_core**: vLLM 只需要 `outlines_core`，而 `outlines` 包要求旧版本，两者冲突
- **xformers**: 安装时会自动拉取，但版本可能与 vLLM 要求的 torch 不兼容
- **numpy 版本**: vLLM 生态中多个包（opencv, numba）对 numpy 有上限要求 (< 2.3)

### 2. 最佳实践

✅ **推荐做法**:
- 使用精确版本号固定核心依赖（torch, vllm）
- 为不同后端创建独立的可选依赖组
- 添加版本上限避免破坏性更新
- 使用 `pip check` 验证依赖完整性

❌ **避免**:
- 不要同时安装 `outlines` 和 vLLM
- 不要在 vLLM 环境中使用过于宽泛的版本范围（如 `torch>=2.7`）
- 不要忽略依赖冲突警告

### 3. 安装命令参考

**最小化安装**:
```bash
pip install -e .
```

**安装 vLLM 后端**:
```bash
pip install -e ".[vllm]"
```

**安装所有功能**:
```bash
pip install -e ".[all]"
```

**开发环境**:
```bash
pip install -e ".[dev]"
```

## 依赖冲突检查清单

在添加新依赖前，检查：

1. ✅ 是否与 vLLM 的 torch 版本要求冲突？
2. ✅ 是否引入了 `outlines` 包？
3. ✅ numpy 版本是否 < 2.3？
4. ✅ 是否需要 CUDA 库？确保版本一致
5. ✅ 运行 `pip check` 验证依赖完整性

## 常见问题

### Q1: 为什么不能使用 torch 2.9.1+？
**A**: vLLM 0.13.0 严格要求 torch==2.9.0，且 torchaudio 和 torchvision 也绑定此版本。

### Q2: 可以使用虚拟环境吗？
**A**: 强烈推荐！使用 conda 或 venv 隔离环境，避免全局污染。

### Q3: 遇到 CUDA 相关错误怎么办？
**A**: 确保系统有兼容的 NVIDIA 驱动。vLLM 会自动安装 CUDA 12.8 相关库。

### Q4: 如何验证安装成功？
**A**: 
```python
import vllm
print(vllm.__version__)  # 应显示 0.13.0
```

## 相关文件

- [pyproject.toml](pyproject.toml) - 项目依赖配置
- [README.md](README.md) - 项目主文档

## 更新日志

- **2026-01-12**: 
  - 重构依赖结构，实现模块化管理
  - 成功解决 vLLM 0.13.0 依赖冲突
  - 添加精确版本约束
  - 创建本安装文档


# 从当前目录的pyproject.toml安装（编辑模式）
pip install -e .

# 从当前目录的pyproject.toml安装（常规模式）
pip install .

# 安装项目及其指定的可选依赖组
pip install -e .[vllm]
pip install -e .[vllm,web,jupyter]

# 安装所有可选依赖
pip install -e ".[all]"



# vLLM gpt-oss-20b 完整安装指南

> **最后更新**: 2026年1月13日  
> **脚本版本**: v1.2 (优化版)  
> **状态**: ✓ 生产就绪

## 🚀 快速开始（推荐）

### 一键部署：5分钟完成全部配置

```bash
cd /home/tester/jihz-gpt-oss-20b
. deploy_vllm_gpt_oss_20b_optimized.sh --all
```

**这个命令会自动完成：**
1. ✓ 创建 conda 环境（Python 3.12）
2. ✓ 安装 vLLM 0.13.0 及所有依赖（使用国内镜像加速）
3. ✓ 安装 gpt-oss 包
4. ✓ 启动 vLLM 服务器（http://localhost:8010）

**完整耗时：** 10-15分钟（取决于网络速度）

---

## 📋 部署脚本参考

### 脚本位置
- **主脚本**: `deploy_vllm_gpt_oss_20b_optimized.sh` (推荐)
- **旧脚本**: `deploy_vllm_gpt_oss_20b.sh` (兼容)

### 支持的命令选项

```bash
# 创建环境
. deploy_vllm_gpt_oss_20b_optimized.sh --create-env

# 安装 vLLM（需要激活环境）
conda activate gpt-oss-20b
. deploy_vllm_gpt_oss_20b_optimized.sh --install-vllm

# 安装 gpt-oss
. deploy_vllm_gpt_oss_20b_optimized.sh --install-gpt-oss

# 启动服务器
. deploy_vllm_gpt_oss_20b_optimized.sh --start-server

# 全部执行
. deploy_vllm_gpt_oss_20b_optimized.sh --all
```

---

## 🔧 配置管理

### 镜像源配置

**默认**: 清华大学镜像（中国最快）

编辑脚本第 15 行更换镜像源：

```bash
# 打开脚本
nano deploy_vllm_gpt_oss_20b_optimized.sh

# 修改 PIP_MIRROR 变量为：
# 中科大
PIP_MIRROR="https://mirrors.ustc.edu.cn/pypi/web/simple"

# 或阿里云
PIP_MIRROR="https://mirrors.aliyun.com/pypi/simple/"

# 或腾讯云
PIP_MIRROR="https://mirrors.cloud.tencent.com/pypi/simple"
```

### 网络参数微调

若网络不稳定，编辑脚本第 16-17 行：

```bash
PIP_TIMEOUT=300   # 改为5分钟
PIP_RETRIES=10    # 改为重试10次
```

---

## ✅ 验证安装

### 1. 检查 vLLM

```bash
conda activate gpt-oss-20b
python3 -c "import vllm; print(f'✓ vLLM {vllm.__version__}')"
```

预期输出: `✓ vLLM 0.13.0`

### 2. 检查关键依赖

```bash
python3 << 'EOF'
import torch
import transformers
import vllm
print(f'torch: {torch.__version__}')
print(f'transformers: {transformers.__version__}')
print(f'vllm: {vllm.__version__}')
print(f'CUDA available: {torch.cuda.is_available()}')
EOF
```

预期输出:
```
torch: 2.9.0
transformers: 4.57.3
vllm: 0.13.0
CUDA available: True
```

---

## 🚀 启动服务

### 标准启动

```bash
conda activate gpt-oss-20b
vllm serve openai/gpt-oss-20b --port 8010
```

### 后台运行

```bash
# 使用 nohup
nohup vllm serve openai/gpt-oss-20b --port 8010 > server.log 2>&1 &

# 使用 tmux
tmux new-session -d -s vllm-server "conda activate gpt-oss-20b && vllm serve openai/gpt-oss-20b --port 8010"

# 查看日志
tail -f server.log
```

### 测试 API

```bash
# 列出模型
curl http://localhost:8010/v1/models

# 发送对话请求
curl http://localhost:8010/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "openai/gpt-oss-20b",
    "messages": [{"role": "user", "content": "Hello!"}],
    "max_tokens": 100,
    "temperature": 0.7
  }'

  curl -s http://localhost:8010/v1/models | python3 -m json.tool | head -30
```

---

## 🐛 故障排查

### ❌ 下载超时

**症状**: `Connection timed out while downloading`

**解决方案**:
```bash
# 1. 增加超时时间（编辑脚本，改PIP_TIMEOUT为300秒）
# 2. 更换镜像（见上文配置管理）
# 3. 检查网络连接
ping www.baidu.com
curl -I https://pypi.tuna.tsinghua.edu.cn/simple
```

### ❌ 导入失败

**症状**: `ModuleNotFoundError: No module named 'vllm'`

**原因**: 环境未激活或安装失败

**解决方案**:
```bash
# 检查环境激活
conda activate gpt-oss-20b

# 检查安装
pip show vllm

# 重新安装
pip install --force-reinstall vllm==0.13.0
```

### ❌ CUDA 不可用

**症状**: `RuntimeError: CUDA/cuDNN initialization failed`

**检查步骤**:
```bash
# 检查 GPU
nvidia-smi

# 检查 CUDA
nvcc --version

# 检查 PyTorch CUDA
python3 -c "import torch; print(torch.cuda.is_available())"
```

**回退方案** (CPU 模式，较慢):
```bash
vllm serve openai/gpt-oss-20b --port 8010 --device cpu
```

### ❌ 内存不足

**症状**: `RuntimeError: CUDA out of memory`

**解决方案**:
```bash
# 降低 GPU 内存使用率
vllm serve openai/gpt-oss-20b --port 8010 --gpu-memory-utilization 0.7

# 或使用量化（4-bit）
vllm serve openai/gpt-oss-20b --port 8010 --quantization awq
```

---

## 📊 核心依赖版本表

| 包 | 版本 | 说明 |
|---|---|---|
| vllm | 0.13.0 | 推理引擎 |
| torch | 2.9.0 | PyTorch（严格要求） |
| transformers | 4.57.3+ | 模型加载 |
| torchaudio | 2.9.0 | 音频处理 |
| torchvision | 0.24.0 | 视觉处理 |
| numpy | ≥1.24.0, <2.3 | 数值计算 |
| tokenizers | ≥0.22.0 | 分词器 |
| outlines_core | 0.2.11 | 结构化输出 |

---

## 🔍 脚本优化历程

### v1.2 (2026-01-13) - 当前版本 ✓
- ✓ **移除 uv 改用 pip** - 更稳定，标准工具
- ✓ **集中配置管理** - 镜像、超时、重试在脚本顶部
- ✓ **详细日志输出** - 使用 `-vv` 参数显示下载进度
- ✓ **完善错误处理** - 失败时显示备选方案
- ✓ **改进用户体验** - 进度提示、视觉分隔线、友好错误消息

### 改进对比

| 项目 | v1.0 | v1.2 |
|---|---|---|
| 包管理器 | uv pip | pip ✓ |
| 超时 | 30秒 | 180秒 ✓ |
| 镜像配置 | 硬编码 | 变量集中 ✓ |
| 日志 | 默认 | -vv详细 ✓ |
| 错误处理 | 无 | 状态检查+提示 ✓ |

---

## 💡 性能优化建议

### GPU 内存优化

```bash
# 降低内存使用（快速测试）
vllm serve openai/gpt-oss-20b --port 8010 --gpu-memory-utilization 0.7

# 最大化吞吐量（生产环境）
vllm serve openai/gpt-oss-20b --port 8010 --gpu-memory-utilization 0.9
```

### 并发优化

```bash
# 增加并发处理能力
vllm serve openai/gpt-oss-20b --port 8010 \
  --max-num-seqs 256 \
  --max-num-batched-tokens 20000
```

### 量化加速（内存紧张时）

```bash
# AWQ 量化（4-bit）
vllm serve openai/gpt-oss-20b --port 8010 --quantization awq

# GPTQ 量化
vllm serve openai/gpt-oss-20b --port 8010 --quantization gptq
```

---

## 📚 常见问题

**Q: 安装需要多长时间?**  
A: 首次安装 10-15 分钟（取决于网络和硬件）。后续更新会更快。

**Q: 可以离线安装吗?**  
A: 可以，先在有网络的机器上下载 wheels，再传输到离线环境。

**Q: 支持哪些 GPU?**  
A: 支持 NVIDIA GPU（计算能力 ≥ 7.0），如 V100, A100, RTX 系列。

**Q: 如何使用自定义模型?**  
A: `vllm serve ./path/to/local/model --port 8010`

**Q: 如何启用调试日志?**  
A: `VLLM_LOG_LEVEL=DEBUG vllm serve openai/gpt-oss-20b --port 8010`

---

## 📖 相关文档

- [DEPLOYMENT_SUMMARY.md](DEPLOYMENT_SUMMARY.md) - 脚本优化总结
- [pyproject.toml](pyproject.toml) - 依赖配置文件
- [README.md](README.md) - 项目主文档
- [vLLM 官方文档](https://docs.vllm.ai/)

---

## 📞 获取帮助

遇到问题？按以下顺序排查：

1. 查看 **🐛 故障排查** 部分
2. 检查 `server.log` 日志文件
3. 尝试更换镜像源
4. 查看 [vLLM GitHub Issues](https://github.com/vllm-project/vllm/issues)

---

**最后更新**: 2026年1月13日  
**维护者**: gpt-oss 项目团队

### Get PCI_BUS_ID
```
nvidia-smi --query-gpu=index,name,pci.bus_id --format=csv
index, name, pci.bus_id
0, NVIDIA T1000 8GB, 00000000:47:00.0
1, NVIDIA RTX PRO 6000 Blackwell Workstation Edition, 00000000:5E:00.0
2, NVIDIA RTX PRO 6000 Blackwell Workstation Edition, 00000000:75:00.0

$ lspci | grep -i nvidia
47:00.0 VGA compatible controller: NVIDIA Corporation TU117GL [T1000 8GB] (rev a1)
47:00.1 Audio device: NVIDIA Corporation Device 10fa (rev a1)
5e:00.0 VGA compatible controller: NVIDIA Corporation Device 2bb1 (rev a1)
5e:00.1 Audio device: NVIDIA Corporation Device 22e8 (rev a1)
75:00.0 VGA compatible controller: NVIDIA Corporation Device 2bb1 (rev a1)
75:00.1 Audio device: NVIDIA Corporation Device 22e8 (rev a1)

```
### vllm with PCI_BUS_ID
```
CUDA_DEVICE_ORDER=00000000:5E:00.0 CUDA_VISIBLE_DEVICES=1 vllm --version
```
### 获取GPU的算力
```
nvidia-smi --query-gpu=name,compute_cap --format=csv
NVIDIA T1000 8GB, 7.5
NVIDIA RTX PRO 6000 Blackwell Workstation Edition, 12.0
NVIDIA RTX PRO 6000 Blackwell Workstation Edition, 12.0

```

### hf model download
```
  581  hf download openai/gpt-oss-20b --local-dir ./gpt-oss-20b
  582  HF_ENDPOINT=https://hf-mirror.com hf download openai/gpt-oss-20b --local-dir ./gpt-oss-20b
```
## vLLM Serve (local model)

**启动脚本**: [start_vllm.sh](start_vllm.sh)

完整命令示例：

在.bashrc 中 添加如下环境变量
```bash

export HF_ENDPOINT=https://hf-mirror.com
export PYTORCH_ALLOC_CONF="expandable_segments:True"
export CUDA_VISIBLE_DEVICES=1

nohup conda run -p /home/tester/miniconda3/envs/gpt-oss-env \
  vllm serve "./gpt-oss-20b" --port 8010 \
    --dtype bfloat16 --max-model-len 2048 \
    --tensor-parallel-size 1 --gpu-memory-utilization 0.5 \
    > ./log.txt 2>&1 &

echo $! > vllm.pid
```

**常用操作**:
- 查看日志: `tail -f ./log.txt`
- 停止服务: `kill $(cat vllm.pid)` 或 `pkill -f "vllm serve"`

**关键参数调优**:
- `--dtype bfloat16`: 匹配模型的 mxfp4 量化格式
- `--gpu-memory-utilization 0.5`: 显存不足时降低此值避免 OOM
- `--tensor-parallel-size 1`: 单 GPU 推理
- `HF_ENDPOINT`: 国内镜像加速 HuggingFace 下载

**离线方案**: 若需 Harmony 编码，在有网络的机器执行 `python -c 'from openai_harmony import load_harmony_encoding, HarmonyEncodingName; load_harmony_encoding(HarmonyEncodingName.HARMONY_GPT_OSS)'` 下载缓存，再复制 `~/.cache/huggingface/hub` 到目标机器。

---

## vLLM 详细使用指南

### 什么是vLLM?

vLLM是由加州大学伯克利分校开发的高性能大语言模型(LLM)推理引擎，主要特性包括：
- **OpenAI兼容API**: 可直接替代OpenAI API，支持所有标准接口
- **高效GPU利用**: 通过分页注意力机制(PagedAttention)大幅提升吞吐量(提升10-40倍)
- **支持多种模型**: 包括GPT、Llama、Qwen、Mistral等数百种开源模型
- **灵活部署**: 支持张量并行、管道并行、数据并行等多种分布式配置
- **动态批处理**: 智能批量处理请求，无需等待，极大降低延迟

### 核心概念

#### 1. **推理模式对比**

| 模式 | 说明 | 优势 | 劣势 | 场景 |
|------|------|------|------|------|
| **Prefill(填充)** | 处理用户输入提示词 | 计算密集，GPU利用率高 | 延迟较高 | 处理长上文 |
| **Decode(解码)** | 逐个生成输出tokens | 延迟低，流式生成 | 显存占用多 | 流式输出 |
| **PagedAttention** | 分页缓存KV | 显存效率高(节省90%) | 实现复杂 | 生产环境(默认) |

#### 2. **并行策略**

| 策略 | 配置项 | 说明 | 使用场景 |
|------|--------|------|---------|
| **张量并行** | `--tensor-parallel-size N` | 将模型层分割到N个GPU | 大模型，多GPU机器 |
| **管道并行** | `--pipeline-parallel-size N` | 将模型阶段分割到N个GPU | 超大模型，多节点 |
| **数据并行** | `--data-parallel-size N` | 多个完整副本处理不同数据 | 并发请求多的场景 |
| **专家并行** | `--enable-expert-parallel` | MoE模型的专家分割 | Mixtral、DeepSeek等MoE模型 |

### vLLM serve 完整参数详解

#### **模型配置 (ModelConfig)**

```bash
# 基础模型参数
--model <MODEL_ID>                  # 模型名称或本地路径，默认Qwen/Qwen3-0.6B
--served-model-name <NAME>          # API中返回的模型名称，可指定多个名字
--dtype {auto,float32,float16,bfloat16}  # 数据精度
                                         # auto: 自动选择 (推荐)
                                         # float32: 单精度，显存占用最多
                                         # float16: 半精度，平衡精度和显存
                                         # bfloat16: 16位浮点，精度与范围平衡
--quantization <METHOD>             # 量化方法
                                     # None: 不量化 (默认)
                                     # awq: AWQ int4量化
                                     # gptq: GPTQ int4量化
                                     # fp8: FP8量化 (需CUDA 11.8+)
                                     # aqlm: AQLM量化

# 序列长度控制
--max-model-len <LENGTH>            # 最大序列长度 (token数)
                                     # 影响：显存占用、处理能力
                                     # 默认：从模型config自动检测
                                     # 支持人类可读格式: 1k(1000), 2K(2048), 25.6k(25600)

# 权重加载
--revision <VERSION>                # HuggingFace模型版本 (分支/标签/commit)
--tokenizer <TOKENIZER_PATH>        # 自定义分词器路径
--tokenizer-mode {auto,hf,slow,mistral}  # 分词器模式
--trust-remote-code                 # 是否信任远程代码 (某些模型需要)

# 生成配置
--generation-config <PATH>          # 生成配置文件路径
--override-generation-config <JSON> # 覆盖生成配置
                                     # 示例: --override-generation-config '{"temperature": 0.7}'

# 高级功能
--enable-prompt-embeds              # 支持输入文本嵌入(prompt_embeds)
--skip-tokenizer-init               # 跳过分词器初始化(只接受token IDs)
--enforce-eager                     # 强制使用eager模式(不使用CUDA图)
--disable-sliding-window            # 禁用滑动窗口
```

**使用示例**：
```bash
# 最基本的启动
vllm serve openai/gpt-oss-20b

# 使用bfloat16精度，限制序列长度为2048
vllm serve openai/gpt-oss-20b --dtype bfloat16 --max-model-len 2048

# 指定API中的模型名称，支持FP8量化
vllm serve openai/gpt-oss-20b --served-model-name my-gpt-oss --quantization fp8
```

---

#### **并行配置 (ParallelConfig)**

```bash
# 张量并行 (最常用)
--tensor-parallel-size <N>          # GPU数量，用于分割模型层
                                     # 默认: 1 (单GPU)
                                     # 建议: <= GPU总数

# 管道并行 (大模型必需)
--pipeline-parallel-size <N>        # 将模型分为N个阶段
                                     # 通常与张量并行配合使用

# 数据并行 (并发场景)
--data-parallel-size <N>            # 数据并行副本数
--data-parallel-backend {mp,ray}    # 后端: mp(多进程) 或 ray(分布式)
                                     # mp: 本地多进程，速度快
                                     # ray: 支持多机分布式

# 上下文并行 (长序列优化)
--decode-context-parallel-size <N>  # 解码阶段的上下文并行
--prefill-context-parallel-size <N> # 填充阶段的上下文并行

# 专家并行 (MoE模型)
--enable-expert-parallel            # 启用专家并行
--expert-placement-strategy {linear,round_robin}  # 专家放置策略

# 分布式设置 (多节点)
--distributed-executor-backend {mp,ray,uni}  # 分布式后端
--master-addr <ADDRESS>             # 主节点地址，默认127.0.0.1
--master-port <PORT>                # 主节点通信端口，默认29501
--nnodes <N>                        # 总节点数 (多机必需)
--node-rank <RANK>                  # 当前节点排名 (0=主节点)
```

---

#### **缓存配置 (CacheConfig)**

```bash
# GPU显存利用率
--gpu-memory-utilization <FLOAT>    # GPU显存使用比例 (0.0-1.0)
                                     # 默认: 0.9 (90%)
                                     # 项目推荐: 0.4-0.5 (保留余量)
                                     # 越高: 显存利用率越高，延迟越低
                                     # 越低: 更稳定，发生OOM风险小

# KV缓存优化
--kv-cache-dtype {auto,bfloat16,fp8}  # KV缓存的数据类型
                                        # auto: 与模型dtype相同 (默认)
                                        # fp8: FP8量化，节省显存75%
                                        # bfloat16: 高精度

--kv-cache-memory-bytes <SIZE>      # KV缓存大小 (字节数)
                                     # 支持人类可读: 10G, 512M, 1k
                                     # 若指定此项，gpu-memory-utilization无效

# 块配置 (内部优化)
--block-size {1,8,16,32,64,128,256}  # KV缓存块大小 (token数)
                                      # CUDA默认最大32，ROCm最大64
                                      # 通常无需修改

# 前缀缓存 (多轮对话优化)
--enable-prefix-caching              # 启用前缀缓存
                                     # 优势: 重复前缀共享，减少显存
                                     # 场景: 多轮对话、检索增强等

# CPU卸载 (显存不足方案)
--cpu-offload-gb <GiB>              # CPU卸载空间 (GiB)
                                     # 虚拟扩展GPU显存
                                     # 示例: --cpu-offload-gb 10
                                     # 需要快速CPU-GPU互联

# CPU交换
--swap-space <GiB>                  # CPU交换空间，默认4GiB
                                     # KV缓存无法放在GPU时用CPU缓存
```

---

#### **前端服务器配置 (Frontend)**

```bash
# 服务器基础设置
--host <HOSTNAME>                   # 绑定地址，默认None(0.0.0.0)
--port <PORT>                       # 监听端口，默认8000
                                     # 示例: --port 8010

# API密钥 (安全认证)
--api-key <KEY1> <KEY2> ...        # 所需API密钥
                                     # 客户端请求头: Authorization: Bearer <KEY>
                                     # 示例: --api-key sk-123abc sk-456def

# CORS跨域配置
--allowed-origins <ORIGIN1> <ORIGIN2> ...  # 允许的源
                                            # 默认: ['*'] (允许所有)
--allowed-methods <METHOD1> ...    # 允许的HTTP方法
--allowed-headers <HEADER1> ...    # 允许的HTTP头
--allow-credentials                # 是否允许cookies

# OpenAPI文档
--disable-fastapi-docs             # 禁用Swagger UI和ReDoc
                                     # 生产环境建议禁用

# 聊天模板
--chat-template <PATH_OR_STRING>    # 自定义聊天模板文件路径
--chat-template-content-format {auto,string,openai}  # 内容格式

# 日志和监控
--enable-log-requests              # 记录请求
--enable-log-outputs               # 记录生成输出
--log-config-file <PATH>           # 日志配置文件
--disable-uvicorn-access-log       # 禁用HTTP访问日志
--log-error-stack                  # 记录错误堆栈

# SSL/TLS (HTTPS)
--ssl-keyfile <PATH>               # SSL私钥文件
--ssl-certfile <PATH>              # SSL证书文件
--ssl-ca-certs <PATH>              # CA证书文件
--ssl-cert-reqs {none,optional,required}  # 客户端证书要求

# LoRA支持 (参数高效微调)
--lora-modules <NAME1=PATH1> ...   # LoRA模块配置
```

---

### 项目实际配置详解 (start_vllm.sh)

根据您的脚本，以下是关键参数的实际应用：

```bash
# 环境变量配置
export CUDA_VISIBLE_DEVICES=1,2              # 使用GPU 1和2
export CUDA_DEVICE_ORDER=PCI_BUS_ID          # 按PCI总线顺序排列(避免混乱)
export PYTORCH_ALLOC_CONF=expandable_segments:True  # 可扩展显存分段
export TIKTOKEN_CACHE_DIR="./o200k_base"     # 本地分词器缓存
export HF_ENDPOINT=https://hf-mirror.com     # 国内HF镜像加速

# vLLM启动命令详解
vllm serve ./gpt-oss-20b \
  --port 8010                      # API端口: http://localhost:8010/v1
  --served-model-name gpt-oss-20b  # API返回的模型名称
  --dtype bfloat16                 # BFloat16精度(与模型量化格式匹配)
  --max-model-len 2048             # 最大上下文长度2048 tokens
  --tensor-parallel-size 1         # 单GPU运行(TP=1)
  --gpu-memory-utilization 0.4     # 仅使用40%显存(保留60%余量)
```

**参数调优建议**：

| 参数 | 当前值 | 增加吞吐 | 降低延迟 | 节省显存 |
|------|--------|---------|---------|---------|
| `gpu-memory-utilization` | 0.4 | → 0.7-0.9 | → 0.3-0.5 | → 0.1-0.2 |
| `max-model-len` | 2048 | → 4096 | → 1024 | → 512 |
| `dtype` | bfloat16 | - | - | → fp8 |
| `tensor-parallel-size` | 1 | → 2 | - | → 1 |

---

### API调用方式

#### **Python客户端** (推荐)

```python
from openai import OpenAI

# 创建客户端
client = OpenAI(
    base_url="http://localhost:8010/v1",
    api_key="any-key"  # vLLM本地部署无需真实key
)

# 1. 简单对话
response = client.chat.completions.create(
    model="gpt-oss-20b",
    messages=[
        {"role": "user", "content": "你好，请介绍一下自己"}
    ]
)
print(response.choices[0].message.content)

# 2. 流式输出 (适合长生成)
stream = client.chat.completions.create(
    model="gpt-oss-20b",
    messages=[{"role": "user", "content": "讲个故事"}],
    stream=True
)
for chunk in stream:
    print(chunk.choices[0].delta.content or "", end="")

# 3. 指定生成参数
response = client.chat.completions.create(
    model="gpt-oss-20b",
    messages=[{"role": "user", "content": "1+1=?"}],
    temperature=0.7,        # 创意度 (0.0=确定, 1.0=随机)
    top_p=0.9,             # nucleus采样
    max_tokens=100,        # 最多生成100个token
    top_k=50               # top-k采样
)
```

#### **Curl命令行**

```bash
# 查看可用模型
curl http://localhost:8010/v1/models

# 简单对话
curl http://localhost:8010/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-oss-20b",
    "messages": [
      {"role": "user", "content": "你好"}
    ]
  }'

# 流式输出
curl http://localhost:8010/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-oss-20b",
    "messages": [{"role": "user", "content": "讲个笑话"}],
    "stream": true
  }'

# 设置API密钥
curl -H "Authorization: Bearer sk-123abc" \
  http://localhost:8010/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-oss-20b",
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

---

### API 返回值详解

#### **模型列表查询示例**

```bash
curl http://localhost:8010/v1/models
```

**返回值结构**：

```json
{
  "object": "list",                    // 返回类型：列表
  "data": [                            // 模型数据数组
    {
      "id": "gpt-oss-20b",             // ✓ 模型ID/名称
      "object": "model",               // ✓ 对象类型
      "created": 1768516601,           // ✓ 创建时间戳（Unix秒）
      "owned_by": "vllm",              // ✓ 所有者
      "root": "/home/tester/jihz-gpt-oss-20b/gpt-oss-20b",  // ✓ 模型文件路径
      "parent": null,                  // 父模型（无）
      "max_model_len": 2048,           // ✓ 最大序列长度
      "permission": [                  // ✓ 权限配置
        {
          "id": "modelperm-8538fe73f27d95dc",
          "object": "model_permission",
          "created": 1768516601,
          "allow_create_engine": false,    // ✗ 不允许创建引擎
          "allow_sampling": true,          // ✓ 允许采样（生成）
          "allow_logprobs": true,          // ✓ 允许获取logprobs
          "allow_search_indices": false,   // ✗ 不允许搜索
          "allow_view": true,              // ✓ 允许查看
          "allow_fine_tuning": false,      // ✗ 不允许微调
          "organization": "*",             // 所有组织可用
          "group": null,                   // 无特定分组
          "is_blocking": false             // 不阻止
        }
      ]
    }
  ]
}
```

**核心含义解释**：

| 字段 | 值 | 说明 |
|------|-----|------|
| **响应状态** | 200 OK | ✓ 服务正常运行 |
| **可用模型数** | 1个 | `gpt-oss-20b` 已加载 |
| **支持功能** | 生成 + logprobs | 可用于 chat/completions 和 completions 端点 |
| **最大上下文** | 2048 tokens | 单次请求最多 2048 个 token |
| **权限配置** | - | 模型允许采样和查询，但不支持微调 |

**性能指标提示**：
- 当 `allow_sampling: true` 时，可以使用概率采样生成多样化回复
- 当 `allow_logprobs: true` 时，可以获取每个 token 的概率分布
- `max_model_len: 2048` 限制了单次请求的输入+输出总长度

---

### 性能优化和故障排查

详见 [troubleshooting.md](troubleshooting.md) 文档，包括：
- **常见问题与解决方案**（显存溢出、推理缓慢、API无响应等）
- **快速诊断命令**（一键检查服务状态）
- **性能优化参数**（显存、吞吐量、延迟调优）

#### **快速故障检查**

```bash
# 检查服务状态
./start_vllm.sh --status

# 查看实时日志
tail -f log.txt

# 测试API连接
curl http://localhost:8010/v1/models

# 完整诊断
echo "=== GPU状态 ===" && nvidia-smi && \
echo "=== 进程检查 ===" && ps aux | grep vllm && \
echo "=== API测试 ===" && curl -s http://localhost:8010/v1/models | python3 -m json.tool | head -20
```

---

### 快速参考表

#### **快速启动命令**

```bash
# 单GPU标准配置
vllm serve openai/gpt-oss-20b --port 8010 --dtype bfloat16 --max-model-len 2048

# 双GPU高性能
vllm serve openai/gpt-oss-20b --port 8010 --tensor-parallel-size 2 --gpu-memory-utilization 0.8

# 生产环境推荐
vllm serve openai/gpt-oss-20b \
  --port 8010 \
  --tensor-parallel-size 2 \
  --gpu-memory-utilization 0.7 \
  --max-model-len 4096 \
  --api-key prod-key-123 \
  --enable-log-requests

# 极限显存压缩 (16GB显存运行20B)
vllm serve openai/gpt-oss-20b \
  --dtype bfloat16 \
  --max-model-len 1024 \
  --gpu-memory-utilization 0.3 \
  --cpu-offload-gb 10 \
  --kv-cache-dtype fp8
```

#### **环境变量速查**

```bash
# 日志级别
VLLM_LOG_LEVEL={INFO,DEBUG,WARNING,ERROR}

# GPU相关
CUDA_VISIBLE_DEVICES=0,1           # 指定GPU
CUDA_DEVICE_ORDER=PCI_BUS_ID       # 按PCI总线顺序排列
PYTORCH_ALLOC_CONF=expandable_segments:True  # 可扩展显存

# HuggingFace
HF_TOKEN=hf_xxx                    # 访问私有模型
HF_ENDPOINT=https://hf-mirror.com  # 镜像加速
HF_HOME=/path/to/cache             # 缓存目录
```

---

## Streamlit 介绍

### Streamlit 概述

**Streamlit** 是一个开源的 Python 框架，用于快速构建和部署 Web 应用程序，特别是用于数据科学、机器学习和 AI 应用。它使用纯 Python 代码即可创建交互式前端界面，无需编写 HTML、CSS 或 JavaScript。

**核心特点：**
- **零前端知识**: 只需编写 Python 代码，自动生成 Web UI
- **快速开发**: 代码改动自动热重载，无需手动刷新
- **丰富组件**: 支持图表、表格、输入框、滑块、文件上传等
- **部署简单**: 一行命令即可启动应用或部署到云端
- **活跃社区**: 有大量第三方组件和示例

### Streamlit 在 gpt-oss 项目中的应用

在 gpt-oss 项目中，Streamlit 被用于创建交互式聊天界面，具体应用如下：

#### 1. **聊天应用** (`examples/streamlit/streamlit_chat.py`)
```python
streamlit run streamlit_chat.py
```

**功能特性：**
- 💬 **实时聊天界面** - 支持与 gpt-oss-20b 模型的流式对话
- 🎚️ **参数调整** - 侧边栏支持调节：
  - 模型选择（large/small）
  - 推理难度（low/medium/high）
  - 温度（Temperature）- 控制输出随机性
  - 最大输出令牌数
  
- 🛠️ **高级功能**：
  - 自定义系统指令
  - 函数调用支持
  - 浏览器搜索集成
  - Python 代码执行器
  - 推理过程可视化

- 📊 **调试工具**：
  - 实时日志显示
  - 消息状态查看
  - API 响应监控

#### 2. **音频波形显示** (`examples/test/audio_wave.py`)
```python
streamlit run audio_wave.py
```

**功能特性：**
- 🎤 **麦克风输入** - 实时捕获音频
- 📈 **波形可视化** - 使用 Matplotlib 绘制音频波形
- 📊 **音频分析** - 显示采样率、时长、振幅、RMS 能量等统计信息

### Streamlit 工作原理

1. **脚本执行模式**: Streamlit 从上到下执行 Python 脚本，将输出转换为 Web 组件
2. **会话状态**: 通过 `st.session_state` 保持用户交互的状态（如消息历史、用户输入等）
3. **实时刷新**: 用户交互时自动重新运行脚本，更新界面
4. **流式处理**: 支持流式输出，用于实时显示模型生成的文本

### 常用 Streamlit 组件

| 组件 | 用途 | 示例 |
|------|------|------|
| `st.title()` | 标题 | `st.title("💬 Chatbot")` |
| `st.sidebar` | 侧边栏 | `st.sidebar.slider("参数", 0, 100)` |
| `st.chat_message()` | 聊天气泡 | `with st.chat_message("user"): st.write("你好")` |
| `st.chat_input()` | 聊天输入框 | `prompt = st.chat_input("输入消息...")` |
| `st.session_state` | 状态管理 | `st.session_state.messages = []` |
| `st.columns()` | 列布局 | `col1, col2 = st.columns(2)` |
| `st.button()` | 按钮 | `if st.button("点击"): ...` |
| `st.slider()` | 滑块 | `value = st.slider("选择", 0, 100)` |
| `st.expander()` | 可展开区域 | `with st.expander("更多"): ...` |
| `st.pyplot()` | 显示图表 | `st.pyplot(fig)` |

### 快速开始

#### 安装 Streamlit 和依赖

**安装基础包：**
```bash
pip install streamlit matplotlib numpy
```

**安装 PyAudio（用于音频处理）**

PyAudio 需要系统级依赖。不同系统安装方式如下：

**Ubuntu/Debian:**
```bash
# 安装系统依赖
sudo apt-get update
sudo apt-get install -y portaudio19-dev python3-dev

# 然后使用 pip 安装
pip install pyaudio
```

**macOS:**
```bash
# 使用 Homebrew
brew install portaudio

# 然后使用 pip 安装
pip install pyaudio
```

**Windows:**
```bash
# 直接使用 pip 安装（通常自带依赖）
pip install pyaudio

# 如果失败，可以下载预编译的 wheel 文件
# 从 https://files.pythonhosted.org/packages/ 下载对应版本的 .whl 文件
# 然后运行：pip install xxx.whl
```

#### 创建简单应用
创建文件 `app.py`:
```python
import streamlit as st

st.title("我的第一个 Streamlit 应用")
name = st.text_input("你叫什么名字?")
if name:
    st.write(f"你好，{name}! 👋")
```

#### 运行应用
```bash
streamlit run app.py
```

应用会在浏览器中启动，通常访问 `http://localhost:8501`

### gpt-oss 项目中的集成示例

**运行聊天应用：**
```bash
cd examples/streamlit
streamlit run streamlit_chat.py
```

**访问地址：**
- 本地: `http://localhost:8501`
- 网络: `http://192.168.0.115:8501`

**交互流程：**
1. 在浏览器输入框输入消息
2. Streamlit 捕获输入，添加到 `st.session_state.messages`
3. 发送请求到 vLLM API (`http://192.168.0.115:8010/v1/responses`)
4. 流式接收模型回复，实时更新聊天界面
5. 支持推理过程可视化、函数调用等高级功能

### 调试和优化

**查看实时日志：**
- 启用 Debug mode 查看消息状态
- 检查 Streamlit 终端输出（包含 `[HH:MM:SS]` 时间戳的日志）

**常见问题解决：**
- **状态码 400**: 检查 API 请求格式（如 `metadata.__debug` 必须是字符串）
- **超出最大长度**: 降低 `max_output_tokens` 或清理消息历史
- **UI 不更新**: 确保使用 `st.session_state` 而非普通变量

### 扩展资源

- [Streamlit 官方文档](https://docs.streamlit.io/)
- [Streamlit 组件库](https://streamlit.io/components)
- [Streamlit 社区示例](https://streamlit.io/gallery)

### 扩展阅读

- [vLLM官方文档](https://docs.vllm.ai/)
- [vLLM GitHub仓库](https://github.com/vllm-project/vllm)
- [性能优化指南](https://docs.vllm.ai/en/latest/getting_started/cpu-offloading.html)
- [量化指南](https://docs.vllm.ai/en/latest/quantization/overview.html)

最后更新: 2026年1月16日
